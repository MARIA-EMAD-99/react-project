import { Outlet } from "react-router-dom";

const Profile = () => {
    return (
      <>
        <div>Product</div>
        <Outlet/>
      </>
    );
  };
  
  export default Profile;
  